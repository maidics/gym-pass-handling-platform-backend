Goals:
    - have one qr code assigned to one gym and have to user scan that => if no tickets/ passes are available throw or return what
        - why not have a qr code per pass => more complexity for the same problem - requires a gym staff member to scan the user's qr code 
        instead the user has to scan the code and show the result to the gym staff or requires scanner hardware

Passes:
    - UsePass endpoint: - CurrentUser, GymAdministrator
        - consume Gym qr code and passId to use (have frontend decide if the user has a valid Pass for this gym or if they have any)
            - if a user has multiple passes to the same gym let them decide which one to use
            - warn user if they already have a pass for that gym
        - call .Use() on Pass instance

Gyms: 
    - RegisterGym endpoint - AppAdministrator
    - UpdateGymProfile endpoint - GymAdministrator

ApplicationUser:
    - UpdateUserProfile endpoint

UserGymMembership:
    

TODO:
    - add default AppAdministrator account
    - add logic to ban users from gyms => ApplicationUser.GymMemberStatus: GymMemberStatus enum (member, banned)
    - add enpoint to ban users from gyms => policy CanBanMember - GymAdministrator && AppAdministrator - send email of this
    - add default GymMemberStatus when user buys pass
    - check if a user is banned when using passes
    - parse incoming enums to actual enums
    - change Price properties to decimal and config EFCore to use decimal(18, 2)
    
Questions:
    - how are transactions handled in apps like this?
    - how to handle AppAdmin accounts? who can register and how? have a default one seeded into db?
    - how are backends designed?

Answers:
    - difference between endpointgroupbase .RequireAuthorization() and [Authorize]: 
        - .RequireAuthorization() - in ca it checks for Authentication, 
        - [Authorize] - used for policies in CQRS files so they're more maintainable 